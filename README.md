# Symbol Autocomplete Examples

This repo demonstrates how symbols declared by external libraries only appear in autocomplete after they have been explicitly imported from the origin library.

- run `pnpm install`
- navigate to either `symbols-autocomplete.ts` or `symbols-no-autocomplete.ts` to test the symbol autocomplete behavior.
